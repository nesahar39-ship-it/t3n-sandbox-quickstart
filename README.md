# T3N Sandbox Quickstart

A minimal Terminal 3 Agent Developer Kit connection demo for the T3N sandbox.

- Uses testnet only
- No real payments or wallet access
- API keys are read only from the local environment and are never committed

## Run

Install dependencies, set T3N_API_KEY in your local terminal session, then run npm start. A successful run prints the assigned did:t3n: identifier.
